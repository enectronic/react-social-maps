'use strict';

describe('LocatorStore', function() {
  var store;

  beforeEach(function() {
    store = require('stores/LocatorStore.js');
  });

  it('should be defined', function() {
    expect(store).toBeDefined();
  });
});
